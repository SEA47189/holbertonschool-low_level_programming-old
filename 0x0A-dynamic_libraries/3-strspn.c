#include "main.h"
#include <stdio.h>
/**
* _strspn - gets length of prefix substring
*
*@s: character to print
*@accept: length of substring
*Return: 0 - success
*/
unsigned int _strspn(char *s, char *accept)
{
return (0);
}
