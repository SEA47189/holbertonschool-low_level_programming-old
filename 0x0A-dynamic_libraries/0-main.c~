#include "main.h"
#include <stdio.h>

/**
* main - check the code
*_strlen - find string length
* Return: Always EXIT_SUCCESS.
*/
int main(void)
{
printf("%d\n", _strlen("My Dyn Lib"));
}
return (0);
}
